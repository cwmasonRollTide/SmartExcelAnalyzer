services:
  mongodb:
    image: mongo:5.0
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
      MONGO_INITDB_DATABASE: smartexcelanalyzer
    ports:
      - "27017:27017"
    volumes:
      - mongodata:/data/db

  llm-service:
    build:
      context: ./LLM
      dockerfile: llm.Dockerfile
    ports:
      - "8000:8000"
    command: uvicorn main:app --host 0.0.0.0 --port 8000
    volumes:
      - model_cache:/root/.cache/huggingface
      - ./LLM:/app
    environment:
      - TEXT_GENERATION_MODEL=facebook/bart-large-cnn
      - EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
      - DB_CONNECTION_STRING=mongodb://admin:password@mongodb:27017/smartexcelanalyzer

  backend:
    build: 
      context: ./Backend
      dockerfile: backend.Dockerfile
    ports:
      - "5001:80"
    environment:
      # ASP.NET Core settings
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:80
      
      # LLMServiceOptions
      - LLMServiceOptions__COMPUTE_BATCH_SIZE=500
      - LLMServiceOptions__LLM_SERVICE_URL=http://llm-service:8000
      
      # DatabaseOptions
      - DatabaseOptions__SAVE_BATCH_SIZE=500
      - DatabaseOptions__MAX_CONNECTION_COUNT=400
      - DatabaseOptions__MAX_RETRY_COUNT=3
      - DatabaseOptions__ConnectionString=mongodb://admin:password@mongodb:27017
      - DatabaseOptions__DatabaseName=smartexcelanalyzer
    depends_on:
      - llm-service
      - mongodb
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    volumes:
      - ./Backend:/app
      - /app/obj
      - /app/bin

  frontend:
    build:
      context: ./Frontend
      dockerfile: frontend.Dockerfile
    ports:
      - "3000:3000"
    environment:
      - VITE_BASE_API_URL=http://localhost:5001
    volumes:
      - ./Frontend:/app
      - /app/node_modules
    depends_on:
      - backend

volumes:
  model_cache:
  mongodata:



  # Uncomment the following section to use PostgreSQL for some type of audit logging down the line
  # db:
  #   image: postgres:14
  #   environment:
  #     POSTGRES_DB: smartexcelanalyzer
  #     POSTGRES_USER: admin
  #     POSTGRES_PASSWORD: password
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U admin -d smartexcelanalyzer"]
  #     interval: 120s
  #     timeout: 5s
  #     retries: 5
  #     start_period: 10s
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data


  # healthcheck:
    #   test: ["CMD", "curl", "-f", "http://localhost:80/health"]
    #   interval: 120s
    #   timeout: 10s
    #   retries: 3
    #   start_period: 40s